Implement https://github.com/fatb4f/factory/issues/104.

Treat the following sources as authority, in order:

1. https://github.com/fatb4f/factory/issues/103
2. https://github.com/fatb4f/factory/issues/104
3. AGENTS.md
4. Vetted CUE contracts created for the implementation unit

Issue transport:

- Resume implementation on the GitHub-linked #104 issue branch. The user prompt
  may simply request continuation; do not require issue bodies in
  `UserPromptSubmit`.
- Read stable authority from issue #104's body and mutable continuation state
  from its single comment marked `<!-- factory-progress:v1 -->`; then inspect
  the branch, working tree, and validated workflow evidence.
- Keep authority transport separate from tracking transport. Authority records
  the issue body, explicit authority revision when declared, and body digest.
  Tracking records the progress-comment ID and update time, node statuses,
  current node, commit, validations, blockers, and next action.
- Do not use GitHub issue `updatedAt` as an authority revision. Do not regenerate
  authority transport for routine progress-comment edits.
- At session end, edit the existing marked progress comment. Change the issue
  body only through an explicit authority revision, and regenerate authority
  transport only after such a change. Regenerate #103 only when its upstream
  authority revision changes.
- Treat offline JSON, plans, and tracking projections as machine-local transient
  context. Never commit them or treat them as architectural authority. Keep
  admission evidence under the declared runtime evidence root.

Implementation unit:

    kg://repository/factory/implementation-unit/uv-bd-bootstrap/v1

Use the workflow below as a transient Codex execution plan. It guides node
selection and prompting but does not replace the issues, AGENTS.md, or CUE as
architectural or admission authority.

Execution rules:

- Verify the pinned issue #103 transport revision, body digest, and normalized
  bootstrap snapshot before consuming subordinate declarations.
- Execute only nodes whose dependencies have validated evidence.
- Independent runnable nodes may execute concurrently.
- Stay within issue #104's authoritative artifact inventory, except for this
  explicitly requested prompt.txt seed.
- Update CUE declarations and workflow contracts before Python-assigned nodes.
- Preserve the legacy context resolver, its inline metadata, and the active
  UserPromptSubmit path.
- Use disposable environments for locked/exact or mutation fixtures.
- Treat generated prompts, reports, and evidence as transient projections.
- Do not accept claimant-supplied validity or admission booleans.
- Stop on an unresolved authority reference, invalid dependency, undeclared
  mutation, failed validation, missing evidence, or non-true admission export.
- CUE alone computes admission; shell and Python success are evidence only.

Workflow DAG:

    snapshot.verify
        -> workbook-identity.admit
        -> project.create

    project.create
        -> lock.create

    snapshot.verify + workbook-identity.admit
        -> contracts.create

    lock.create + contracts.create
        -> workbook.create

    contracts.create
        -> fixtures.create

    workbook.create + fixtures.create
        -> bootstrap.execute
        -> self-conformance.admit
        -> provisional.retire

Node objectives:

snapshot.verify
    Resolve #103 and #104 from the current execution's structured issue
    transport, then verify the pinned #103 transport and normalized bootstrap
    snapshot. Refresh missing, stale, or unverifiable issue data from GitHub.

workbook-identity.admit
    Encode and validate the issue-selected bootstrap workbook identity.

project.create
    Replace the empty pyproject.toml placeholder with the canonical Factory
    uv project.

lock.create
    Generate uv.lock from the project and prove `uv lock --check` succeeds.

contracts.create
    Define the closed bootstrap, workflow, scenario, evidence, command, and
    output contracts under marimo/workflows/bdd/.kb.

workbook.create
    Implement only Python-assigned workflow nodes in
    marimo/workflows/bdd/validate_implementation_unit.py. Do not add PEP 723
    metadata or admission logic.

fixtures.create
    Implement all positive, negative, invariant, compatibility, and
    adversarial scenarios declared by issue #104.

bootstrap.execute
    Run locked and exact validation in disposable runtime state and admit the
    resulting transient evidence through CUE.

self-conformance.admit
    Execute the canonical suite against this implementation unit and require
    the CUE-exported self-conformance admission value to be literally true.

provisional.retire
    Prove provisional admission is retired and unavailable to later or
    repeated implementation units. This is the terminal node.

For each selected node, construct a bounded working prompt containing only:

- implementation-unit and node identity;
- applicable authority references;
- requirement and acceptance-criterion IDs;
- allowed artifacts and constraints;
- required inputs and outputs;
- exact validation commands;
- required completion evidence.

Execute the selected node, validate its evidence, and advance only through the
declared DAG. The stable operator validation entrypoint must be:

    just validate-bdd-bootstrap

Completion requires every issue #104 admission condition, including literal
true self-conformance admission and proof of provisional-admission retirement.
