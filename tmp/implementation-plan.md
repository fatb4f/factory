# Issue #104 implementation plan

Status: transient, generated, non-authoritative

Implementation unit: `kg://repository/factory/implementation-unit/uv-bd-bootstrap/v1`

Branch: `104-bootstrap-locked-uv-project-and-self-conformant-bdd-contract`

Authority order:

1. `https://github.com/fatb4f/factory/issues/103`
2. `https://github.com/fatb4f/factory/issues/104`
3. repository `AGENTS.md`
4. vetted CUE contracts created by this unit

Context input: sibling `requirements-source.json`, with stable issue-body
authority separated from the mutable `factory-progress:v1` tracking comment.

## Initial state and stop conditions

- The repository is on the GitHub-linked #104 branch with a clean worktree.
- `pyproject.toml` and `uv.lock` are zero-length placeholders.
- `marimo/workflows/bdd/` does not exist yet.
- The current `AGENTS.md` digest is
  `sha256:e0d75da9fbd27c4b09fe5e91079897acb0c0f1ec5cfb9b283e0b35b25160eb29`.
- Issue #104 declares the current `AGENTS.md` digest
  `sha256:e0d75da9fbd27c4b09fe5e91079897acb0c0f1ec5cfb9b283e0b35b25160eb29`.
- Issue #104 pins the current #103 body digest
  `sha256:005d6c97fa23106624e8149bcdd91614e194d294a255a6fb2936147003e99d81`.
- The current #104 authority-body digest is
  `sha256:ff4bb5349b851cfa2356e679cf2c1e16ce5142ec8999154d478426f176552d54`.
- Mutable progress is read from comment `4954295448`, marked
  `factory-progress:v1`; its timestamps and contents do not participate in
  authority or admission identity.
- Stop on any unresolved authority reference, invalid dependency, undeclared
  mutation, failed validation, missing evidence, or admission export other than
  literal `true`.

## Allowed authoritative implementation surfaces

- `AGENTS.md`
- `prompt.txt` only as the explicitly requested seed
- `pyproject.toml`
- `uv.lock` (generated by uv, never edited manually)
- `justfile`
- `marimo/workflows/bdd/validate_implementation_unit.py`
- `marimo/workflows/bdd/.kb/cue.mod/module.cue`
- `marimo/workflows/bdd/.kb/contract.cue`
- `marimo/workflows/bdd/.kb/bootstrap_workbook_identity.cue`
- `marimo/workflows/bdd/.kb/bootstrap.cue`
- `marimo/workflows/bdd/.kb/workflow.cue`
- `marimo/workflows/bdd/.kb/evidence.cue`
- `marimo/workflows/bdd/.kb/commands.cue`
- `marimo/workflows/bdd/.kb/output.cue`
- `marimo/workflows/bdd/.kb/fixtures/positive/`
- `marimo/workflows/bdd/.kb/fixtures/negative/`

Do not modify the legacy resolver during this unit. Preserve its PEP 723
metadata and active `UserPromptSubmit` behavior.

## Requirement closure

Directly satisfied and complete local closure:

`UV-01`, `UV-02`, `UV-03`, `UV-04`, `BD-01`, `BD-02`, `BD-03`, `BD-04`,
`BD-05`, `BD-06`, `BD-07`, `BD-08`.

Every corresponding `-A1` acceptance ID must have its issue-declared scenario
coverage and omission rationale. No other requirement may be claimed.

## Execution DAG

### 1. `snapshot.verify`

Dependencies: none.

Inputs: structured #103/#104 transport, `AGENTS.md`, repository revision and
working-tree identity.

Work:

- Verify repository, issue numbers, URLs, update times, bodies, and body digests.
- Reconcile #104's pinned #103 revision/body digest with the current #103
  transport.
- Reproduce the normalized `factory.requirements-snapshot.v1` digest for the
  twelve-requirement closure.
- Update online #104 when its declared transport or `AGENTS.md` identity is
  stale, then regenerate `requirements-source.json` before continuing.

Output: verified requirement-source and normalized-snapshot observations.

Gate: all pinned identities reproduce exactly. Python or shell success alone is
not admission.

### 2. `workbook-identity.admit`

Dependencies: `snapshot.verify`.

Acceptance: `UV-03-A1` identity portion.

Work:

- Create the minimal CUE module and
  `bootstrap_workbook_identity.cue` first.
- Encode the issue-selected implementation-unit and workbook identities.
- Bind the decision to the verified current `AGENTS.md` digest.
- Prove the project-managed BDD workbook is distinct from the legacy resolver.

Output: vetted/exported bounded workbook identity decision.

### 3. `project.create`

Dependencies: `workbook-identity.admit`.

Acceptance: `UV-01-A1`.

Work: replace the empty `pyproject.toml` with the canonical Factory project,
Python constraint, and direct dependencies required by the issue-declared
Marimo/Pydantic/Hypothesis/CUE-facing boundary. Do not introduce a second
dependency authority.

Output: candidate project metadata and digest.

### 4. `lock.create`

Dependencies: `project.create`.

Acceptance: `UV-02-A1`.

Work: generate, never hand-edit, `uv.lock`; verify consistency and digest.

Required gate:

```bash
uv lock --check
```

Output: candidate lock identity usable only by this bounded bootstrap unit after
minimal verification.

### 5. `contracts.create`

Dependencies: `snapshot.verify`, `workbook-identity.admit`.

Acceptance: `BD-01-A1`, `BD-02-A1`, `BD-03-A1`, `BD-04-A1`, `BD-05-A1`,
`BD-06-A1`, plus contract portions of `BD-07-A1` and `BD-08-A1`.

Work:

- Define closed/versioned scenario, workflow, fixture, evidence, bootstrap,
  command, and output contracts.
- Declare the exact validation DAG and prove acyclicity, reference closure,
  requirement ordering, production-before-consumption, evidence ordering, and
  terminal unit admission.
- Bind evidence to every identity required by `AGENTS.md`.
- Make claimant-supplied validity/admission fields fail closed.
- Keep minimal-bootstrap claims strictly bounded; CUE alone computes admission.

Validation as soon as the package exists:

```bash
cue vet ./marimo/workflows/bdd/.kb
cue export ./marimo/workflows/bdd/.kb -e output
```

Output: independently vetted CUE boundaries and exported workflow/command
projections.

### 6. `workbook.create`

Dependencies: `lock.create`, `contracts.create`.

Acceptance: `UV-03-A1`, `UV-04-A1`, Python-assigned execution portions of
`BD-05-A1` and `BD-06-A1`.

Work:

- Create only `marimo/workflows/bdd/validate_implementation_unit.py`.
- Implement transport and observations for Python-assigned nodes only.
- Require explicit absolute `--provider-root`, `--repo-root`, and
  `--evidence-root` coordinates.
- Emit only declared transient evidence.
- Add no PEP 723 metadata, CUE authority reconstruction, admission logic,
  fallback workflow, or inferred evidence location.

Output: project-managed Marimo workbook bound to the root uv project.

### 7. `fixtures.create`

Dependencies: `contracts.create`.

Acceptance: all twelve `-A1` IDs.

Work:

- Implement all issue-declared positive, negative, invariant, compatibility,
  and adversarial scenarios under the two admitted fixture roots.
- Include stale/missing lock, forbidden workbook metadata, legacy resolver
  compatibility, ambiguous roots, disposable exact sync, explicit offline
  separation, invalid contracts/workflows, coverage omissions, identity
  mismatch, claimant booleans, non-true command results, and repeated/later
  provisional-admission rejection.
- Encode omissions using #104's canonical rationale rule.

Output: fixture digest and complete acceptance-ID coverage projection.

### 8. `bootstrap.execute`

Dependencies: `workbook.create`, `fixtures.create`.

Acceptance: `UV-04-A1`, `BD-07-A1`.

Work:

- Create the thin `just validate-bdd-bootstrap` wrapper from the CUE command
  projection.
- Allocate unique evidence and disposable environment directories.
- Vet/export every authoritative CUE boundary independently.
- Run positive and expected-failure fixtures.
- Admit concrete evidence through `evidence.cue` and compute only the bounded
  provisional result in CUE.

Required execution shape:

```bash
UV_PROJECT_ENVIRONMENT="$temporary_environment" \
uv run --project "$factory_provider_root" --locked --exact -- \
python "$factory_provider_root/marimo/workflows/bdd/validate_implementation_unit.py" \
  --provider-root "$factory_provider_root" \
  --repo-root "$consumer_root" \
  --evidence-root "$evidence_root"
```

Stable operator gate:

```bash
just validate-bdd-bootstrap
```

Output: revision-bound transient evidence records declared by #104.

### 9. `self-conformance.admit`

Dependencies: `bootstrap.execute`.

Acceptance: `BD-07-A1`, `BD-08-A1`.

Work: execute the canonical suite against this exact implementation unit,
validate its evidence through CUE, and require the exported self-conformance
admission value to be literal boolean `true`.

Output: admitted self-conformance evidence and admitted project/lock identity.

### 10. `provisional.retire`

Dependencies: `self-conformance.admit`.

Acceptance: `BD-08-A1`.

Work: prove provisional admission is retired for this unit and rejected for a
repeat execution or any later unit. This is the terminal DAG node.

Output: CUE-admitted provisional-retirement evidence and literal `true` final
unit admission.

## Completion gate

Run the contract-owned command from a clean checkout and accept completion only
when all declared evidence validates, the legacy hook remains operational,
self-conformance exports literal `true`, provisional admission is retired, and
no undeclared or candidate artifact is present in the bootstrap merge.

At session end, edit the designated progress comment with completed nodes,
stable identities, failures, and the next runnable node. Refresh only the
tracking context after routine progress changes. Regenerate authority transport
only after an explicit authority-body revision.
