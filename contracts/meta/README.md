# contracts/meta

`contracts/meta` is the generated-output gate for factory contract projections.

Generated files remain evidence/projection surfaces, not source authority. Source changes belong under the owning contract tree, and generated outputs registered here are validated by meta checks before downstream projection use.

## Smoke validation

```sh
sh contracts/meta/checks/plugin-smoke
```
