# Codex impact evidence

This directory contains non-authoritative observations produced by the scheduled ChatGPT actuator.

- Run evidence: `runs/<run_id>.codex-impact.report.json`
- Current projection: `latest.codex-impact.report.json`

Evidence must preserve separate `main` and `latest-alpha-cli` channel state. Unresolved refs remain unresolved. Publication paths and issue targets are controlled by the factory-local CUE authority.
