{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'd=$PWD; while [ \"$d\" != / ]; do p=$d/marimo/profiles/context-resolver/context_resolver.py; [ -f \"$p\" ] && exec uv run --script \"$p\" --codex-hook --repo-root \"$d\"; d=${d%/*}; done; exit 0'",
            "timeout": 15,
            "statusMessage": "Filtering context graph"
          }
        ]
      }
    ]
  }
}
