package validobservation

// Runner integration tests materialize a closed observation here only in a
// temporary directory; runtime observations are never committed as authority.
fixtureRole: "known-valid-observation-template"
