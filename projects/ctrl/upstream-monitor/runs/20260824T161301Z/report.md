# ctrl upstream impact report

## Run identity

- Run ID: `20260824T161301Z`
- Monitor state: `terminal_success`
- Qualification state: `observation_only`
- Authority revision: `f97d97ea3e8aa9477b0605af51f402e99535addd`
- Publication revision: `not-yet-sealed`
- ctrl context revision: `b5f82c3a5e33d08d1f5d07cb2c9724c7679c7538`
- Bootstrap baseline: `false`

## Subject context

`fatb4f/ctrl@main` is unchanged from the previous admitted run. `spec/` remains the subject qualification authority; the factory `profiles_ctrl` CUE files are monitor authority. Current subject components remain the six entries declared by `control/components.cue`; there is no repo-backed EvaluationWorld, DPI, DiagnosticPacket, or Weaver component to manufacture from the profile's projected architecture.

## Project topology and ownership

The v3 profile now explicitly models ctrl as a qualified-reactive experiment while retaining component-local ownership. `python-intel` remains observation architecture, the PyPI pipeline remains a materialization workload, and `semagrams` remains future context. Weaver is a projected semantic-interface realization, not qualification authority.

## Qualified reactive evaluation kernel

The current profile authority projects:

```text
source-qualified observations
  -> semantic admission
  -> immutable EvaluationWorld
  -> meaning-preserving DPI lowering
  -> monotonic derivation
  -> derived closure
  -> external CUE qualification
  -> qualified | rejected | inconclusive fixpoint
  -> immutable sealed bundle
  -> deterministic projection / effect intent
```

The GitHub adapter cannot execute this kernel. Computed closure is therefore not asserted as qualified, and no projected world/interface node is reported as a concrete subject component.

## Source state

All required ctrl channels resolved independently. New v3 channels were baselined without invented historical deltas: Weaver `main@78b64d8da8e0034374e66a560ae22991b2e88a81`, fsspec `master@c45f6011c37a74e4cf64d4378bad3e0af38382d0`, and Polars `main@f79668eca55ef3f04af3e60878dc022e91b47944`.

Pinned CUE remains `806821e40fae070318600a264d311517e596353b`; CUE master remains forecast-only. SCIP is unchanged. Optional relational/workbench sources advanced independently but cannot become blocking without a materialized local consumer or gate.

## Codex projection graph

Codex `main` advanced 81 commits from `e482cc66aeeedcb9f333a1f5a0a554eb5aea4b36` to `0d9bb6c34c2742ee8bcddfccb6404a447926ff9f`. The delta includes new Browser/Computer Use configuration requirements projected through app-server and generated schemas, a distinct `guardian_review` thread source propagated through protocol/rollout metadata, additional Guardian policy behavior, and credential-redaction work in app-server diagnostics.

These changes traverse the declared protocol -> schema/runtime -> rollout graph. The existing Guardian/executor MCP compatibility gate remains unresolved locally because the subject has not changed and executable policy compatibility evidence is unavailable here.

## Python semantic and operational graph

CPython 3.14 advanced to `7cc0932332f1fe680bb370c314871c0a884293d0`, but the observed delta did not match the declared compiler/regrtest graph surfaces inspected in this run. CPython main remains a separate forecast at `ee1da7ec8e939a998d5ecf2c749d45eb8f7714e4`.

Astral main advanced to `67dfcf04adee0a946f408fb9f4dd14f59a488e8e` and remains analyzer evidence only. SCIP remains the declared cross-file identity spine at `02559b6181bcf7a53e93c80995a798457117c431`; no SCIP identity is promoted to CPython truth.

## Observation and acquisition graph

OpenTelemetry, dlt, fsspec and OTel-Arrow remain acquisition/observation layers only. Runtime telemetry cannot manufacture semantic identity or qualification. dlt/fsspec records remain observations until admission.

## Relational and diagnostic projection

Arrow, DuckDB, Ibis, Polars, Marimo and pydantic-graph remain optional/projected execution or diagnostic substrates. Their current heads are recorded in evidence, but their movement does not define DPI meaning and does not block without a materialized local consumer.

## Semantic interface projection

Weaver is baselined as the first projected `DiagnosticPacket` interface realization. Its current `main` adds a v2 generation lookup helper; this is interface implementation evidence only. No repo-backed DiagnosticPacket consumer exists in `ctrl@main`, so Weaver cannot presently create a blocking local impact.

## Correlation carrier policy

The v3 correlation contract requires `qualification_run_id`, `evaluation_world_id`, `repository_revision`, and `operation_id`. Trace/span identity remains causal only. `sealed_bundle_id` is post-seal identity and may not be projected backward. Baggage remains deny-by-default.

## Current executable frontier

The profile's full current frontier through EvaluationWorld, DPI, CUE qualification, seal, DiagnosticPacket and Weaver projection is not executable through this GitHub adapter. CPython regrtest/local probes, telemetry round-trip, semantic-kernel execution, and Weaver projection were not executed. Qualification therefore remains `observation_only`.

## Critical

### Codex policy boundary remains gated

Codex continued changing Guardian, tool/MCP, browser/computer-use and approval-facing behavior while `ctrl/spec/.codex` remained unchanged. The declared `codex-hooks-tools-policy` surface has a blocking floor. Do not adopt current Codex policy semantics into local assumptions without executable compatibility evidence.

## High

### Codex protocol/schema projection changed

The new Browser/Computer Use requirement structures and `guardian_review` thread-source semantics cross the declared protocol/schema/runtime/rollout projection chain. Local app-server/config projections require reconciliation before current Codex main is treated as compatible.

### ctrl v3 interface baseline established

The profile now requires a Weaver forecast channel and explicit semantic-kernel/upstream bindings. The channel resolves, but the subject has no concrete DiagnosticPacket realization. This is a contract operationalization update, not evidence that Weaver qualifies ctrl worlds.

## Notes

- OTel Python GenAI advanced to `7824027640b9c779acb50999a982e939f5950f9c`, including an OpenAI dependency update; provider instrumentation remains observation-only.
- OTel-Arrow advanced to `3e85c3460361446ebfce99e9f35fffd2dd5ab740`; the tip is dependency maintenance and does not establish an OTLP/OTAP semantic change.
- Optional dlt, Arrow, DuckDB, Marimo, pydantic-graph, uv and jj channels advanced; no blocking propagation is admitted without a declared materialized consumer.

## No local action

CPython 3.14 movement inspected in this run did not traverse the declared compiler/regrtest paths. SCIP, OTel contrib, Ibis and CUE master remain unchanged from the preceding admitted heads where noted in evidence.

## Publication

- Bundle: `contracts/upstream-monitor/ctrl/contract-surface/runs/20260824T161301Z/`
- Manifest: `contracts/upstream-monitor/ctrl/contract-surface/runs/20260824T161301Z/manifest.json`
- Latest pointer: `contracts/upstream-monitor/ctrl/contract-surface/latest.json`
- Export unit: directory

## Validation notes

Authority, subject context, project topology, semantic kernel, graph, correlation carrier policy, interface boundary and publication plan were read. All required channels resolved. CUE execution, CPython regrtest/local probes, Astral/SCIP executable correlation, OTel pipeline, OTLP/OTAP round-trip, relational projection, semantic-kernel execution and Weaver projection were not executable through the GitHub adapter. The report and summary are projections from `evidence.json`; `terminal_success` does not imply executable qualification.
