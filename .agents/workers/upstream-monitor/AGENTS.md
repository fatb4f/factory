# upstream-monitor compatibility pointer

This path is non-normative compatibility only.

The single generic upstream-monitor worker procedure is:

`contracts/workers/upstream-monitor/AGENTS.md`

Do not add execution rules here. Registered unit-local procedures must read the canonical contract-colocated worker procedure directly.
