# Migration Map

This map records the disposition for the mixed migration surface before any
additional destructive pruning. `migration/legacy/**` is retained only as
non-authority reference material; green-path validation must use
`contracts/factory/**` plus the explicitly retained runtime and resolver inputs.

Allowed classifications are `keep-active`, `migrate-factory`,
`quarantine-legacy`, and `delete`.

No root survives because it existed before. Every retained artifact must support
the reflective transition factory.

## Top-Level Roots

| source path | current role | classification | target path | action | blocking dependency | notes |
| --- | --- | --- | --- | --- | --- | --- |
| `.github/` | GitHub issue and repository metadata. | keep-active | `.github/` | Retain as repository shell metadata. | None. | Non-authority support surface. |
| `.codex/` | Repo-local Codex hook and skill projection. | keep-active | `.codex/` | Retain as runtime adapter ingress. | Must remain generated/projection support, not factory semantic authority. | Consumes supplied issue packets and emits bounded prompt context. |
| `README.md` | Repository overview. | keep-active | `README.md` | Retain as root shell documentation. | Must describe factory authority and quarantine boundary. | Current README documents `migration/legacy/` as non-authority evidence. |
| `go.mod` | Go tooling module metadata. | keep-active | `go.mod` | Retain as tooling-only module. | Must not become semantic contract authority. | There is no root `cue.mod`. |
| `justfile` | Developer validation commands. | keep-active | `justfile` | Retain and keep checks aimed at factory pruning surface. | Checks must exclude quarantined legacy paths as green-path inputs. | `just check` rejects old top-level authority roots. |
| `checks/` | Former repo-local smoke checks. | delete | None. | Remove as an independent validation root. | Executable validation must be assertion-backed through `contracts/factory/assertions/**` and `contracts/factory/reflection.cue`. | `generated/checks/agent-context-hook` is the active generated projection. |
| `cmd/` | Active Go command entrypoints for contract tooling. | keep-active | `cmd/` | Retain while commands serve factory workflows. | Commands must not reintroduce root semantic authority. | `contractctl` and `contract-mcp` are tooling adapters only. |
| `internal/` | Active Go implementation support. | keep-active | `internal/` | Retain while backing active command adapters. | Must remain tooling implementation, not contract authority. | Supports current Go tooling. |
| `contracts/` | Contract container for factory authority and active inputs. | keep-active | `contracts/` | Retain as the only active contract container. | Non-factory roots must stay explicitly classified by this map. | Active authority is `contracts/factory/`; runtime and resolver roots remain bounded inputs. |
| `migration/` | Explicit quarantine container. | keep-active | `migration/legacy/` | Retain as migration boundary. | Tests/assertions must not treat quarantine as green-path input. | This root is part of the target shape even though it was not in the original mixed root list. |
| `adapters/git-mcp-go/` | Former raw Git MCP adapter source. | quarantine-legacy | `migration/legacy/adapters/git-mcp-go/` | Keep quarantined as legacy adapter reference. | Worker apertures must remain under `contracts/factory/workers/**` or backed by `cmd/` and `internal/`. | Raw adapter material is not factory authority. |
| `docs/refactor/` | Former refactor guidance. | quarantine-legacy | `migration/legacy/docs/refactor/` | Keep quarantined as legacy reference. | Factory docs must describe factory authority rather than old repo authority. | Not green-path factory documentation. |
| `docs/*.md` | Former top-level legacy documentation files. | quarantine-legacy | `migration/legacy/docs/` | Keep quarantined as legacy reference. | Factory docs must be written under `contracts/factory/docs/`. | Includes old agent, plugin, token, and VCS notes. |
| `fixtures/agent-runtime/` | Runtime packet fixtures. | migrate-factory | `contracts/factory/fixtures/packets/runtime/` | Keep as active factory packet fixtures. | Active fixtures must be tied to factory packets or assertions. | Current active runtime packet registry lives under factory fixtures. |
| `fixtures/resolver/` | Resolver packet fixtures. | migrate-factory | `contracts/factory/fixtures/packets/resolver/` | Keep as active factory packet fixtures. | Resolver fixture shape must remain bound to active resolver inputs. | Current active resolver packet fixtures live under factory fixtures. |
| `fixtures/agent-skill/` | Legacy agent skill fixtures. | quarantine-legacy | `migration/legacy/fixtures/agent-skill/` | Keep quarantined as migration evidence. | Rewrite as factory worker/resolver material before reactivation. | Not core factory surface. |
| `fixtures/mcp/` | Legacy MCP fixtures. | quarantine-legacy | `migration/legacy/fixtures/mcp/` | Keep quarantined as migration evidence. | Active MCP behavior must enter through worker apertures. | Not green-path factory authority. |
| `fixtures/vb-contract/` | Legacy virtual branch contract fixture. | quarantine-legacy | `migration/legacy/fixtures/vb-contract/` | Keep quarantined as migration evidence. | GitButler worker aperture must define any active behavior. | Raw virtual branch contract fixture remains legacy. |
| `fixtures/vcs/` | Legacy VCS fixtures. | quarantine-legacy | `migration/legacy/fixtures/vcs/` | Keep quarantined as migration evidence. | Raw VCS behavior must enter through worker evidence apertures. | Not agent-facing authority. |
| `generated/checks/` | Assertion-backed executable check projections. | keep-active | `generated/checks/` | Retain only generated validation adapters selected from factory assertion inventory. | Must not define semantic invariants independently of `contracts/factory/assertions/**`. | `generated/checks/agent-context-hook` delegates semantics to CUE assertions. |
| `generated/fixtures/` | Fixture projections from materialized assertions. | keep-active | `generated/fixtures/` | Regenerate through `just generate-validation`. | Fixtures must carry reflection provenance and must not be semantic authority. | Active generated fixture is `generated/fixtures/agent-context-hook-prompt.json`. |
| `generated/evidence/` | Exported materialization and runtime validation evidence. | keep-active | `generated/evidence/` | Regenerate materialization evidence through `just generate-validation`; runtime checks export observed packet evidence and assertion results. | Evidence must be reproducible from reflection inventory and generated checks. | Contains materialization report, bounded packet evidence, and assertion result projections. |
| `generated/evidence/control-loop/` | Bounded control-loop stage exports. | keep-active | `generated/evidence/control-loop/` | Regenerate through `just export-validation-loop`. | Stage exports must remain non-authoritative and provenance-stamped from the CUE control model. | Contains input, transform, output, sensor, error-signal, control-action, and next-state evidence. |
| `generated/codex-plugin/` | Old generated Codex plugin output. | quarantine-legacy | `migration/legacy/generated/codex-plugin/` | Keep quarantined as generated legacy evidence. | Generated files must have a factory source before becoming active. | Current material is plugin output, not factory generated output. |
| `generated/factory/` | Future factory generated output. | migrate-factory | `contracts/factory/generated/` | Reserve for generated factory artifacts. | A factory projection/generation source must exist first. | No active files are required in this path yet. |
| `projections/repo/` | Former repo projection output. | quarantine-legacy | `migration/legacy/projections/repo/` | Keep quarantined as legacy projection evidence. | Factory projection generator must exist before any active projection is restored. | Repo projection is not factory authority. |
| `providers/chezmoi/` | Legacy provider declaration. | quarantine-legacy | `migration/legacy/providers/chezmoi/` | Keep quarantined as legacy provider evidence. | Direct provider observation must be replaced by worker apertures. | Not green-path factory authority. |
| `providers/lua-lsp/` | Legacy provider declaration. | quarantine-legacy | `migration/legacy/providers/lua-lsp/` | Keep quarantined as legacy provider evidence. | Direct provider observation must be replaced by worker apertures. | Not green-path factory authority. |
| `providers/cue-lsp/` | CUE provider aperture material. | migrate-factory | `contracts/factory/workers/cue/cue-lsp/` | Keep as factory worker provider evidence. | Worker aperture boundary must remain the active authority. | Current active material is scoped under factory workers. |
| `providers/cue-rg/` | CUE provider aperture material. | migrate-factory | `contracts/factory/workers/cue/cue-rg/` | Keep as factory worker provider evidence. | Worker aperture boundary must remain the active authority. | Current active material is scoped under factory workers. |
| `test/agent-context-hook.sh` | Legacy hook test script. | quarantine-legacy | `migration/legacy/test/agent-context-hook.sh` | Keep quarantined as migration evidence. | Factory checks must replace old repo-layout validation. | Not part of green-path validation. |
| `test/` | Former top-level test root. | delete | None. | Do not restore as an active root. | Factory assertions and `just check` must cover the pruning surface. | Any retained historical script belongs under quarantine. |

## Contract Families

| source path | current role | classification | target path | action | blocking dependency | notes |
| --- | --- | --- | --- | --- | --- | --- |
| `contracts/factory/` | Reflective transition factory authority. | keep-active | `contracts/factory/` | Retain as active authority root. | Local `contracts/factory/cue.mod/` remains the only CUE module boundary if needed. | Contains object, transition, workers, adapters, assertions, fixtures, generated, kernel, and docs. |
| `contracts/agent-context-resolver/` | Active resolver selection and route-planning input. | keep-active | `contracts/agent-context-resolver/` | Retain as active bounded input. | Resolver outputs must not depend on legacy repo/provider authority. | Move later only after the factory resolver package boundary is stable. |
| `contracts/agent-runtime/` | Active runtime event and packet input. | keep-active | `contracts/agent-runtime/` | Retain as active bounded input. | Runtime bindings must remain packet inputs, not raw SDK authority. | Move later only after the factory runtime package boundary is stable. |
| `contracts/adapters/` | Former adapter contract family. | quarantine-legacy | `migration/legacy/contracts/adapters/` | Keep as legacy reference only. | Factory adapters must remain worker aperture boundaries. | Current active adapter boundary is `contracts/factory/adapters/`. |
| `contracts/agent-skill/` | Former agent skill contract family. | quarantine-legacy | `migration/legacy/contracts/agent-skill/` | Keep as migration reference only. | Rewrite as factory worker/resolver material before reactivation. | Legacy fixtures remain under `migration/legacy/fixtures/agent-skill/`. |
| `contracts/assertions/` | Former repo-wide assertion family. | migrate-factory | `contracts/factory/assertions/` | Keep factory pruning assertions active. | Assertions must validate factory pruning rather than old repo layout. | Current active gate is `contracts/factory/assertions/gate.cue`. |
| `contracts/assertions/repo-layout/` | Old repo-layout assertion material. | quarantine-legacy | `migration/legacy/contracts/assertions/` | Keep quarantined as legacy assertion evidence. | Factory gates must not depend on old repo layout assertions. | Legacy assertion files are already quarantined. |
| `contracts/context/packet/` | Former context packet family. | quarantine-legacy | `migration/legacy/contracts/context/packet/` | Keep as resolver migration reference only. | Active context behavior must be represented through resolver/runtime inputs or factory objects. | No independent context authority survives. |
| `contracts/context/` | Former context contract root. | delete | None. | Do not restore as an independent authority root. | Resolver/runtime packet boundaries must carry any active context semantics. | Remaining historical packet material belongs under quarantine. |
| `contracts/graph/` | Former repo graph vocabulary. | quarantine-legacy | `migration/legacy/contracts/graph/` | Keep as reference unless rewritten as factory evidence graph. | A factory evidence graph model would need explicit object/transition binding. | Current graph vocabulary is legacy evidence only. |
| `contracts/object/` | Former object contract family. | migrate-factory | `contracts/factory/object/` | Merge object definitions into factory object authority. | Factory object definitions must cover required transition artifacts. | Current object contracts are active under `contracts/factory/object/`. |
| `contracts/protocols/a2a/` | Former A2A protocol sketch. | quarantine-legacy | `migration/legacy/contracts/protocols/a2a/` | Keep as migration reference only. | Any active protocol must be expressed through worker apertures or runtime packets. | Not green-path factory authority. |
| `contracts/protocols/mcp/` | Former MCP protocol sketch. | quarantine-legacy | `migration/legacy/contracts/protocols/mcp/` | Keep as migration reference only. | Any active MCP behavior must enter through worker apertures. | Not green-path factory authority. |
| `contracts/protocols/` | Former protocol contract root. | delete | None. | Do not restore as an independent authority root. | Active transport semantics must be bound to runtime packets or worker apertures. | Remaining historical protocol material belongs under quarantine. |
| `contracts/providers/` | Former provider declarations. | quarantine-legacy | `migration/legacy/contracts/providers/` | Keep as legacy provider reference. | Direct backend observation must be removed from active contracts. | Current active provider-like CUE references are scoped under factory CUE workers. |
| `contracts/repo/` | Former repo authority model. | quarantine-legacy | `migration/legacy/contracts/repo/` | Keep quarantined until reference value is exhausted. | Factory pruning checks must remain independent of repo authority model. | Must not return as active authority. |
| `contracts/transition-factory/` | Former transition factory family. | migrate-factory | `contracts/factory/transition/` | Merge transition definitions into factory transition authority. | Factory transition gates and invariants must cover retained behavior. | Current transition contracts are active under `contracts/factory/transition/`. |
| `contracts/validation/` | Former validation family. | quarantine-legacy | `migration/legacy/contracts/validation/` | Keep as legacy validation reference. | Active validation must target factory pruning surface through factory assertions. | Legacy validation material is quarantined. |
| `contracts/vcs/` | Former raw VCS authority model. | quarantine-legacy | `migration/legacy/contracts/vcs/` | Keep quarantined until reference value is exhausted. | Raw Git/VCS must be represented only through worker evidence apertures if needed. | Must not return as agent-facing authority. |
| `contracts/workers/codex/` | Codex worker aperture material. | migrate-factory | `contracts/factory/workers/codex/` | Keep as factory worker aperture. | Worker definitions must expose aperture contracts, not raw provider declarations. | Current active material is under factory workers. |
| `contracts/workers/cue/` | CUE worker aperture material. | migrate-factory | `contracts/factory/workers/cue/` | Keep as factory worker aperture. | Worker definitions must expose aperture contracts, not raw provider declarations. | Current active material is under factory workers. |
| `contracts/workers/gitbutler/` | GitButler worker aperture material. | migrate-factory | `contracts/factory/workers/gitbutler/` | Keep as factory worker aperture. | Worker definitions must expose aperture contracts, not raw VCS authority. | Current active material is under factory workers. |
| `contracts/workers/` | Former worker contract root. | delete | None. | Do not restore as an independent authority root. | Worker apertures must live under factory authority. | Remaining active worker material belongs under `contracts/factory/workers/`. |
| `contracts/registry.cue` | Former aggregate registry. | delete | None. | Do not restore as repo-wide semantic registry. | Factory authority must not depend on root contract aggregation. | Old registry behavior is superseded by factory-local surfaces and bounded inputs. |
