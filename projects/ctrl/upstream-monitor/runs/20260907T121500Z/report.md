# ctrl upstream impact report

## Run identity

- Run ID: `20260907T121500Z`
- Monitor state: `terminal_success`
- Qualification state: `observation_only`
- Authority revision: `014e2381d1400e14c50ebfcdf58e01fa8dfdefa2`
- Publication revision: `not-yet-sealed`
- ctrl context revision: `b5f82c3a5e33d08d1f5d07cb2c9724c7679c7538`
- Bootstrap baseline: `false`

## Subject context

`fatb4f/ctrl@main` remains at the same subject revision as the prior admitted run. Its `spec/` remains subject qualification authority; Factory-local profile CUE remains monitor authority.

## Project topology and ownership

The profile topology remains unchanged: ctrl owns the concrete mutation-control/qualification experiment; python-intel remains an architectural observation producer; PyPI/wheel/PEP remains the first materialization workload; semagrams remains future/non-authoritative; Weaver remains a projected semantic-interface implementation.

## Qualified reactive evaluation kernel

The profile-defined EvaluationWorld → meaning-preserving DPI lowering → monotonic closure → external CUE qualification → fixpoint → seal chain remains projected authority. The GitHub actuator did not execute this kernel.

## Source state

All required sources resolved distinctly. Fresh heads include Codex main `ce5c4133…`, CPython 3.14 `a9152fb…`, CPython main `3b564385…`, Astral main `519fe8ff…`, OTel Python core `a447e2fe…`, and Weaver `a4c5f1b2…`. Codex alpha, SCIP, OTel contrib/GenAI/Arrow and CUE master are unchanged from the prior admitted run.

## Codex projection graph

Codex main advanced 58 commits. New user-verification protocol/runtime surfaces, MCP auth-change and elicitation handling, Guardian authorization decisions, approvals and generated-schema changes widen the protocol → schema → runtime/policy boundary. No local executable compatibility witness was available.

## Python semantic and operational graph

CPython 3.14 advanced four commits and now contains the nested inlined-comprehension compiler fix in `Python/codegen.c`/compiler internals. This moves a previously forecast compiler change into the active 3.14 source baseline. No declared regrtest slice or local differential probe was executed.

Astral main advanced 22 commits with substantial `ty_python_semantic`, importer, reachability, predicate and diagnostic changes. These remain analyzer/correlation evidence and do not override CPython runtime semantics.

## Observation and acquisition graph

Required source/channel identity and revision were preserved. External source state remains observation evidence only; no telemetry or acquired record was promoted to qualification authority.

## Relational and diagnostic projection

No Arrow/DuckDB/Ibis/Polars or Marimo projection was executed. Their profile role remains replaceable projection/execution substrate beneath CUE-governed semantics.

## Semantic interface projection

Weaver advanced only through UI/package housekeeping in this delta. No new DiagnosticPacket semantic-interface claim is admitted, and Weaver remains non-authoritative for qualification.

## Correlation carrier policy

The required semantic identity remains `qualification_run_id`, `evaluation_world_id`, `repository_revision`, and `operation_id`; trace/span identity remains causal only and baggage remains deny-by-default.

## Current executable frontier

The full Codex-turn → candidates → admission → immutable EvaluationWorld → static/runtime differential evidence → OTel correlation → DPI lowering → monotonic derivation → CUE qualification → fixpoint → sealed bundle → DiagnosticPacket → Weaver → Codex feedback frontier remains unexecuted by the GitHub App. `otel-causal-correlation` and `otlp-otap-roundtrip` witnesses were not run.

## Critical

- **Codex user-verification, Guardian and MCP policy boundary widened.** Keep the executable Codex policy/tool gate open.

## High

- **CPython 3.14 active compiler baseline changed.** Run the declared compiler/regrtest and local probes before behavioral qualification.
- **Astral ty semantic/importer surface changed materially.** Refresh correlation expectations; analyzer output remains observation-only.

## Notes

No additional note-level fresh deltas were admitted.

## No local action

OTel Python core and Weaver fresh changes are housekeeping; SCIP, OTel contrib/GenAI/Arrow and CUE master are unchanged from the prior admitted source state.

## Publication

- Bundle: `projects/ctrl/upstream-monitor/runs/20260907T121500Z/`
- Manifest: `projects/ctrl/upstream-monitor/runs/20260907T121500Z/manifest.json`
- Latest pointer: `projects/ctrl/upstream-monitor/latest.json`
- Export unit: directory

## Validation notes

Authority, current subject context, topology, semantic kernel, source model, graph, correlation policy, interface boundary and publication plan were read. All required source channels resolved. CUE execution, CPython regrtest/probes, Astral/SCIP correlation, OTel pipeline, OTLP/OTAP round-trip, relational projection, semantic-kernel execution and Weaver projection were not executable through the GitHub actuator. Report and summary are projections from `evidence.json`.
