# Repository context fixture

```cue semantic-ref
authority: {
  id: "factory.repository-context"
  contract: "contracts/state/repository-context.cue"
}
subject: "factory.repository-context"
kind: "substrate"
```
